{
  "name": "nyt-food",
  "private": true,
  "workspaces": ["extension", "server", "scripts"],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:extension": "npm run dev --workspace=extension",
    "build:extension": "npm run build --workspace=extension"
  }
}
